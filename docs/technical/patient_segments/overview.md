# patient_segments — overview

Clinic-local patient tags ("Groups") for patient grouping and future
campaign targeting. A segment is a named label owned by one clinic;
patients are linked to segments. There is deliberately no points
ledger, no redemption, and no expiry — grouping only.

Surfaced inline on the patient summary (`patient.summary.cards` slot);
no standalone nav entry.
